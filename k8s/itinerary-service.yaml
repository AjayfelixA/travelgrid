apiVersion: apps/v1
kind: Deployment
metadata:
  name: itinerary-service
  namespace: travelgrid
  labels:
    app: itinerary-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: itinerary-service
  template:
    metadata:
      labels:
        app: itinerary-service
    spec:
      containers:
      - name: itinerary-service
        image: yourdockerhub/itinerary-service:latest
        ports:
        - containerPort: 3000
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 250m
            memory: 256Mi

---

apiVersion: v1
kind: Service
metadata:
  name: itinerary-service
  namespace: travelgrid
spec:
  type: ClusterIP
  selector:
    app: itinerary-service
  ports:
  - port: 3000
    targetPort: 3000
